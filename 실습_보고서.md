# MSA 기반 온라인 강의 수강 서비스 보고서

ㅇ반 ㅇㅇㅇ 이름

---

## Chapter 1. 고객 중심 서비스 이해

### 1. 서비스 개요

> 📝 작성 필요 — 서비스 개요에 대한 코멘트를 주시면 정리하겠습니다.

### 2. 고객에게 제공하는 가치

> 📝 작성 필요

### 3. 주요 고객 적용 사례

> 📝 작성 필요

### 4. 고객이 느낄 수 있는 한계

> 📝 작성 필요

### 5. 구조 전환 제안

> 📝 작성 필요

---

## Chapter 2. 시스템 기술 구조 및 설계 이유

### 1. 왜 MSA(마이크로서비스 아키텍처)인가

단일 서버(모놀리식) 구조에서는 회원, 강의, 수강신청, 결제, 추천 기능이 하나의 애플리케이션에 묶여 있어, 기능 하나를 수정·배포할 때도 전체 시스템을 다시 빌드하고 재기동해야 하는 부담이 있습니다.
본 프로젝트는 이 문제를 해결하기 위해 도메인별로 서비스를 분리하고, 각 서비스가 독립적으로 개발·배포·확장될 수 있는 MSA 구조로 설계되었습니다.

### 2. 사용 기술 스택

| 구성 요소 | 기술 | 역할 |
|---|---|---|
| 서비스 디스커버리 | Eureka Server | 각 마이크로서비스를 등록하고, 서비스 간 위치(IP/포트)를 동적으로 조회 |
| 인증 서버 | Auth Server | 로그인/토큰 발급 등 인증 담당 |
| API 게이트웨이 | API Gateway | 클라이언트 요청을 받아 각 서비스로 라우팅하는 단일 진입점 |
| 회원 서비스 | user-service (Spring Boot) | 회원 가입/조회 등 사용자 관리 |
| 강의 서비스 | course-service (Spring Boot) | 강의 정보 등록/조회 |
| 수강신청 서비스 | enrollment-service (Spring Boot) | 수강 신청/이력 관리 |
| 결제 서비스 | payment-service (Spring Boot) | 결제 처리 |
| 추천 서비스 | recommend-service | 강의 추천 |
| 메시지 브로커 | Kafka (confluentinc/cp-kafka) | 서비스 간 비동기 이벤트 처리 |
| 데이터베이스 | MariaDB | 각 서비스의 데이터 저장 |
| 프론트엔드 | Vue.js (vue-frontend) | 사용자 화면(UI) |
| 컨테이너 오케스트레이션 | Docker / Docker Compose | 전체 서비스를 컨테이너로 묶어 로컬/서버에서 일괄 기동 |

### 3. 시스템 기동 순서

```
MariaDB / Kafka (인프라)
  → Eureka (서비스 등록)
    → Auth Server (인증)
      → API Gateway + 4개 서비스 (user / course / enrollment / payment)
        → Recommend Service
```

서비스 간 `depends_on` 관계를 기반으로, 인프라(DB, 메시지 브로커) → 서비스 레지스트리 → 인증/게이트웨이 → 개별 도메인 서비스 순으로 기동됩니다. 앞 단계가 정상적으로 뜨지 않으면 뒷 단계 서비스가 Eureka 등록이나 인증에 실패할 수 있는 구조입니다.

### 4. 서비스별 포트

| 서비스 | 포트 |
|---|---|
| MariaDB | 3379 (컨테이너 내부 3306) |
| Kafka | 9092 |
| Eureka Server | 8761 |
| Auth Server | 9000 |
| API Gateway | 8080 |
| user-service | 8081 |
| course-service | 8082 |
| enrollment-service | 8083 |
| payment-service | 8084 |
| recommend-service | 8085 |
| Vue Frontend | 3000 |

---

## Chapter 3. 트러블슈팅 사례 정리

### 1. `docker compose build` 중 Docker Hub Pull Rate Limit (429 Too Many Requests) 발생

- **증상**: `docker compose build --no-cache` 실행 중, user-service 등 각 서비스 Dockerfile의 빌드 스테이지(`FROM eclipse-temurin:21-jdk-alpine AS builder`)에서 이미지를 받아오지 못하고 빌드가 실패함

  ```
  target user-service: failed to solve: eclipse-temurin:21-jdk-alpine:
  failed to resolve source metadata for docker.io/library/eclipse-temurin:21-jdk-alpine:
  unexpected status from HEAD request to
  https://registry-1.docker.io/v2/library/eclipse-temurin/manifests/21-jdk-alpine:
  429 Too Many Requests
  ```

- **원인**: Docker Hub는 익명(비로그인) 사용자의 이미지 pull·메타데이터 조회 요청 횟수를 시간당 일정 수준으로 제한합니다. `--no-cache` 옵션으로 여러 서비스(user, course, enrollment, payment, recommend)를 연달아 재빌드하면서 동일한 base image(`eclipse-temurin:21-jdk-alpine`)를 짧은 시간 안에 반복 요청하게 되어 이 제한(429)에 걸림. 실제로도 `docker compose build --no-cache`를 네 차례 연속 재시도했지만 매번 동일하게 실패함.

- **해결 과정**:
  1. 반복적인 재빌드 자체가 원격 레지스트리 요청을 계속 발생시켜 제한을 다시 유발한다는 것을 확인 → 매 실행마다 원격에서 base image를 새로 받아오는 방식을 포기
  2. 이미 정상적으로 빌드된 이미지들을 미리 저장해 둔 아카이브(`msa-lecture-images.tar.gz`, 공통 인프라 이미지는 `infra-images.tar`)를 `docker load -i`로 로컬 Docker 이미지 캐시에 직접 로드
  3. 컴포즈 설정을 용도별로 분리: 기존 빌드용 정의는 `docker-compose.build.yml`로 보존하고, 로컬에 로드된 이미지만 사용하는 실행용 `docker-compose.yml`을 별도로 구성
  4. `docker compose up -d --no-build --pull never` 옵션으로 컨테이너를 기동하여 원격 레지스트리 접근 자체를 차단 → 정상 기동 확인

- **배운 점**: 매번 `--no-cache`로 전체 서비스를 재빌드하기보다, 변경이 없는 서비스는 재빌드를 피하고 base image를 로컬에 캐시/보관해 재사용하는 것이 레지스트리 요청 제한을 피하는 데 효과적임. `--pull never` / `--no-build` 옵션은 "빌드된 이미지를 그대로 재사용"하려는 의도를 compose 실행 단계에서 명시적으로 강제할 수 있어 유용함.

---

### ✅ 트러블슈팅 요약

- Docker Hub는 익명 사용자의 pull(메타데이터 조회 포함) 요청에 rate limit(429)을 걸어두므로, 여러 서비스를 반복 재빌드할 때 특히 주의가 필요함
- 매번 원격에서 이미지를 새로 받기보다, 한 번 빌드한 이미지는 `docker save` / `docker load`로 아카이빙해 재사용하는 전략이 안정적임
- 빌드용 compose 파일과 실행용 compose 파일을 분리해두면, 트러블슈팅 상황에서도 원인(빌드 vs 실행)을 빠르게 구분할 수 있음

---

## 프로젝트 진행 노트 (회의록 · 느낀점)

### 2026-08-10 — 주제 아이디어 회의

- 처음에는 이번 과제를, 주어진 백엔드 코드에 약간의 주제 변주를 주어 애자일·MSA를 경험해보는 실습 정도로 이해하고 있었음
- 그러나 교수님이 기대하는 수준은 그보다 높았음: 단순히 MSA 아키텍처를 잘 구현하는 실습이 아니라, **포트폴리오로 내세울 수 있는 수준의 프로젝트 주제**를 원하셨음
  - AI가 실제로 어떤 가치를(어떤 문제를 어떻게 해결하는지) 만들어낼 수 있는지가 드러나야 함
  - 개발자가 아닌 회사 경영진 앞에서도 설득력을 가질 수 있는 스토리여야 함
- **느낀 점**: 기술 구조(MSA, 서비스 분리, 비동기 처리 등)를 잘 짜는 것만으로는 충분하지 않고, "왜 이 서비스가 필요한가", "AI가 구체적으로 어떤 비즈니스 가치를 만드는가"를 비즈니스 관점에서 설명할 수 있어야 한다는 것을 깨달음
  → 이후 주제를 선정할 때는 기술적 완성도뿐 아니라 AI 활용 가치와 경영진 설득 포인트(비용 절감, 매출 증대, 고객 경험 개선 등)를 함께 고려하기로 함

### 2026-08-10 — 주제 확정

- 논의 끝에, 더 넓은 이해관계자(경영진·인사담당자·직원 등)를 고려하여 **기업마다 꼭 필요한 HR(인사) 업무를 지원하는 플랫폼**을 개발하는 프로젝트로 방향을 확정함
- 기존에 실습해 온 강의 수강 플랫폼(user/course/enrollment/payment/recommend) 구조는 도메인만 HR 업무 영역으로 바뀌는 것이고, MSA로 서비스를 쪼개는 설계 방식 자체는 그대로 재사용할 수 있을 것으로 보임
- 구체적으로 어떤 HR 업무(채용, 근태, 평가, 급여 등 중 어느 영역)를 다룰지는 추후 코멘트로 확정되는 대로 Chapter 1에 반영 예정

##